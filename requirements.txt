# Pipecat Voice Assistant Requirements
# Core package - using 0.0.20 for Python 3.9 compatibility
# Note: For Python 3.10+, you can use pipecat-ai==0.0.36

pipecat-ai[silero,audio]==0.0.20

# Additional packages that pipecat doesn't include
fastapi==0.111.0
uvicorn[standard]==0.30.1
websockets==12.0
python-multipart==0.0.9
requests==2.32.3
sounddevice==0.4.6
numpy==1.26.4
python-dotenv==1.0.1

# Note: The following are installed automatically by pipecat:
# - openai (>=1.74.0)
# - aiohttp (>=3.11.12)
# - aiofiles (>=24.1.0)
# - loguru (>=0.7.3)
# - torch and torchaudio